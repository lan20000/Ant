<!--pages/usercenter/couponlist/couponlist.wxml-->
<scroll-view>
  <text class="total" wx:if="listdata.length<=0">空空如也</text>
  <view wx:for="{{listdata}}" wx:for-index="idx" wx:for-item="itemName" wx:if="{{itemName.status!=3}}" class="list {{ itemName.status==1 ? 'not_txt' : ''}}">
    <view class="top flex">
      <view class="left">
        <text class="prie">{{itemName.token}}</text>
        <text class="u">U点</text>
        <text class="recharge txt_center">{{itemName.title}}</text>

        <view class="triangle"></view>
      </view>
      <view class="right">
        <text class="title ">{{itemName.type==1 ? ',赠送券' : '充值券'}}满{{itemName.fullToken}}U点赠送{{itemName.token}}U点</text>
        <text class="remark ">{{itemName.desc}}</text>
      </view>

    </view>
    <view class="footer flex not">
      <text class="time flex1">有效期：{{itemName.startTime}}-{{itemName.endTime}}</text>
      <text class="status">{{text[itemName.status+1]}}</text>
      <image src="./../../../assets/static/ic_arrowRight.png" style="width:12rpx;height:21rpx;"></image>
    </view>
  </view>
  <!-- <view class="list ">
    <view class="top flex">
      <view class="left">
        <text class="prie">200</text>
        <text class="u">U点</text>
        <text class="recharge txt_center">充值券</text>

        <view class="triangle"></view>
      </view>
      <view class="right">
        <text class="title ">充值满5000U点赠送200U点</text>
        <text class="remark ">满5000U点使用</text>
      </view>

    </view>
    <view class="footer flex ">
      <text class="time flex1">有效期：2019.09.12-2019.12.12</text>
      <text class="status">已使用</text>
    </view>

  </view> -->
  <view class="failure tab-nav-item tab-active">
    <text>已失效的券</text>
  </view>
  <view class="list not_txt" wx:for="{{listdata}}" wx:for-index="idx" wx:for-item="item" wx:if="{{itemName.status!=3}}" class="list {{ itemName.status==1 ? 'not_txt' : ''}}">
    <!-- <view class="top flex">
      <view class="left">
        <text class="prie">{{item.}}</text>
        <text class="u">U点</text>
        <text class="recharge txt_center">充值券</text>

        <view class="triangle"></view>
      </view>
      <view class="right">
        <text class="title ">充值满5000U点赠送200U点</text>
        <text class="remark ">满5000U点使用</text>
      </view>

    </view>
    <view class="footer flex ">
      <text class="time flex1">有效期：2019.09.12-2019.12.12</text>
      <text class="status">已失效</text>
    </view> -->

    <view class="top flex">
      <view class="left">
        <text class="prie">{{itemName.token}}</text>
        <text class="u">U点</text>
        <text class="recharge txt_center">{{itemName.title}}</text>

        <view class="triangle"></view>
      </view>
      <view class="right">
        <text class="title ">{{itemName.type==1 ? ',赠送券' : '充值券'}}满{{itemName.fullToken}}U点赠送{{itemName.token}}U点</text>
        <text class="remark ">{{itemName.desc}}</text>
      </view>

    </view>
    <view class="footer flex not">
      <text class="time flex1">有效期：{{itemName.startTime}}-{{itemName.endTime}}</text>
      <text class="status">{{text[itemName.status+1]}}</text>
      <image src="./../../../assets/static/ic_arrowRight.png" style="width:12rpx;height:21rpx;"></image>
    </view>
  </view>
  <navigator url="/pages/usercenter/Cardexchange/Cardexchange" hover-class="navigator-hover">
    <button class="bt theme">兑换卡券</button>
  </navigator>


</scroll-view>